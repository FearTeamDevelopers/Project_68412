{if !empty($content)}
<div class="whiteBg">
    <div class="historyHeader">
        <span class="historyTitle">{echo $content->pageName}</span>
    </div>
    <p class="historyBody">{echo $content->body}</p>
</div>
{/if}
{else}<div class="whiteBg">Žádná historie</div>{/else}


